{{-- <div class="fi-wi">
    <div class="rounded-lg bg-white p-6 shadow-sm ring-1 ring-gray-950/5 dark:bg-gray-900 dark:ring-white/10">
        <h3 class="mb-6 text-lg font-semibold text-gray-900 dark:text-white">
            Quick Access
        </h3>

        <!-- Quick Access Grid -->
        <div class="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-4">
            @foreach ($this->getQuickAccessButtons() as $button)
                <a href="{{ $button['url'] }}"
                    class="flex flex-col items-center justify-center rounded-lg p-4 text-center transition-all hover:bg-gray-50 dark:hover:bg-gray-800">
                    <div class="mb-2 flex h-12 w-12 items-center justify-center rounded-full {{ $button['bgColor'] }}">
                        {!! $button['icon'] !!}
                    </div>
                    <span class="text-sm font-medium text-gray-900 dark:text-white">
                        {{ $button['label'] }}
                    </span>
                </a>
            @endforeach
        </div>
    </div>
</div> --}}

